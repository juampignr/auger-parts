import configuration from"/appConfig.json";import mysql from"mysql2/promise";export const dynamic="force-dynamic";export async function GET(request,{params:params}){try{const connection=await mysql.createConnection({host:"db.auger.org.ar",user:"mocca",password:"sibyll",database:"PMS"});let[fields,metadata]=await connection.query(`SELECT DISTINCT c.column_name, c.column_type ,t.table_name AS associated_table FROM information_schema.columns c LEFT JOIN information_schema.tables t ON c.column_name = t.table_name WHERE c.table_name = '${params.table}' AND c.column_name NOT IN ('inTime', 'UserID', 'ID')`);return console.log(fields),await connection.end(),Response.json({status:"ok",data:fields})}catch(error){return Response.json({status:"error",error:error.stack})}}export async function POST(request,{params:params}){try{let data=await request.formData();data=Object.fromEntries(data.entries());let parsedData={},templateFields={},oneByOne=!1;const connection=await mysql.createConnection({host:"db.auger.org.ar",user:"PMS",password:"Mi:kARgwpb",database:"PMS"});console.log("### QUERY ###");for(const key in data)if(Object.hasOwnProperty.call(data,key)){const[name,type,template]=key.split(":"),element=data[key];parseInt(template)&&(templateFields[name]=data[key]),parsedData[name]="string"===type?`'${element}'`:element}if(console.log(Object.keys(templateFields).length),Object.keys(templateFields).length){for(let index=0;index<array.length;index++){const element=array[index]}for(let[i,n]=[0,parseInt(data.Avail)];i<n;i++){for(const key in templateFields)if(Object.hasOwnProperty.call(templateFields,key)){const value=templateFields[key].replace("#",i);parsedData[key]=value}console.log(`Inserting one by one: insert into ${params.table}(${Object.keys(parsedData).join(", ")}) values (${Object.values(parsedData).join(", ")})`)}}return await connection.end(),Response.json({status:"ok",data:"changeme"})}catch(error){return console.log("### ERROR ###"),console.log(error),Response.json({status:"error",error:error.stack})}}